import styled from "styled-components";

const Header = styled.header`
   width: 100%;
`

const Container = styled.div`
    max-width: 1110px;
    display: flex;
    justify-content: space-around;
`

const Navbar = styled.div`
    display: flex;

`


export {Header, Container, Navbar};